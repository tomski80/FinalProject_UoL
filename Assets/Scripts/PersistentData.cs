using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class PersistantData
{ 
    // Start is called before the first frame update
    public static bool overlayVisibility = true;
}
